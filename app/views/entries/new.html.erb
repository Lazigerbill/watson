<h1>Result</h1>
<p>Input is processed by Watson and here is the result:</p>

<%= render partial: "form" %>

<%= @input %>

<%= @pipeline %>
<br>

<%= form_tag 'upload', :multipart => true do %>
  <label for="file">Upload text File</label> <br> <br>
  <%= file_field_tag "file" %> <br> <br>
  <%= submit_tag 'Proceed' %>
<% end %>

<br>

<% if @participants %>
	<h3>Corporate Participants</h3>
	<% @participants.each do |person| %>
		<%= radio_button_tag(:selection, person) %>
		<%= label_tag(:selection, person) %>
		<br>
	<% end %>	
	<%#= @participants %>
<% end %>	